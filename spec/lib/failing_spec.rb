require 'rails_helper'

describe 'spec failure' do
  it "will break the build if uncommented" do
    # Uncomment the line below (remove # from the beginning) to break the build
    # fixed it
    # expect(false).to be true
  end
end
